[user]
	name = Steve
	email = github.sel@gmail.com
[color]
	ui = true
	status = auto
	diff = auto
	branch = auto
[core]
	excludesfile = /Users/sel/.gitignore_global
	autocrlf = input
[push]
	default = simple
[alias]
	l = "!git log-hist"
	log-hist = "!git log-pretty --graph"
	log-pretty = "log --pretty='format:%C(blue)%h%C(red)%d%C(yellow) %s %C(green)%an%Creset, %ar'"
